name: Kali Self-Hosted CI

on: [push]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Show system info
        run: |
          uname -a
          hostname
          echo "Running on Kali Linux"
